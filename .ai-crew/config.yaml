version: "3.0"
bundle: e2e-premium
workflow: aidlc
defaults:
  model: claude-sonnet-4
  isolation: worktree
  rules:
    - global
  mcp:
    - multi-provider
    - ouroboros
  locale: en
  runs:
    retention: 5
    auto_archive: true
    context_depth: 1
